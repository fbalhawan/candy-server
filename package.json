{
  "name": "candy-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "clean": "rimraf ./dist/*.js",
    "dev": "node ./dist/server.js",
    "docker": "docker build \"$(pwd)\" -t candy-server",
    "dockerrun": "docker run -p 3000:3000 --name candy-server candy-server"
  },
  "author": "Fouad B.",
  "license": "ISC",
  "dependencies": {
    "@types/mongoose": "^5.11.97",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.1",
    "typescript": "^5.0.4"
  }
}
